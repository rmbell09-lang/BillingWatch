# BillingWatch — Show HN 9AM Prep Checklist
*March 10, 2026 — Tuesday*

**Target post time: 9:00 AM ET sharp** (HN traffic peaks 9-10 AM)
**Time needed: ~20 minutes**

---

## T-20 MIN (8:40 AM) — Start the Server

Open Terminal on Mac Mini. Run these in order:

```bash
# 1. Start BillingWatch backend
cd ~/Projects/BillingWatch
source venv/bin/activate  # or: python3 -m venv venv && pip install -r requirements.txt
python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8080 &

# 2. Confirm it's running
curl http://localhost:8080/health
# Should return: {"status":"ok","service":"BillingWatch"}
```

---

## T-18 MIN (8:42 AM) — Get a Live URL

**Option A — Temp tunnel (fastest, no CF creds needed):**

```bash
# Requires cloudflared + LuLu allowlist (see LULU_SETUP.md)
# Install if needed: brew install cloudflare/cloudflare/cloudflared
cloudflared tunnel --url http://localhost:8080
```

Cloudflare prints: `https://abc-random-xyz.trycloudflare.com`
**Copy that URL. This is your [LIVE_URL].**

LuLu will prompt the first time — click **Allow** and check "Remember this decision."

**Option B — Persistent CF deploy (needs CF creds):**
```bash
CF_API_TOKEN=your_token CF_ACCOUNT_ID=your_account_id python3 deploy_cf.py
```

---

## T-12 MIN (8:48 AM) — Test the Landing Page

```bash
# Open in browser
open https://[YOUR_LIVE_URL]

# Test beta signup form — submit your own email
# Confirm it worked:
sqlite3 ~/Projects/BillingWatch/billingwatch.db "SELECT * FROM beta_signups ORDER BY created_at DESC LIMIT 3;"
```

If no `beta_signups` table: confirm the form JS is hitting `/subscribe` and the server is running.

---

## T-8 MIN (8:52 AM) — Test the Demo Endpoint

```bash
curl https://[YOUR_LIVE_URL]/demo/run/charge_failure_spike
# Should return: {"scenario":"charge_failure_spike","alerts_fired":1,...}

curl https://[YOUR_LIVE_URL]/health
# Should return: {"status":"ok"}
```

Both working = you're good to post.

---

## T-5 MIN (8:55 AM) — Update Post with Live URL

Open: `~/Projects/BillingWatch/docs/SHOW_HN_FINAL.md`

Replace every `[LIVE_URL]` and `[REPLACE WITH URL]` with your actual URL.
Add `?ref=hn` to the HN submission URL.

Also update `~/Projects/BillingWatch/docs/BETA_OUTREACH.md` — replace all `[REPLACE WITH URL]` placeholders with your live URL.

---

## T-0 (9:00 AM) — Post to Hacker News

1. Go to: https://news.ycombinator.com/submit
2. **Title** (copy from SHOW_HN_FINAL.md):
   ```
   Show HN: BillingWatch – self-hosted Stripe anomaly detector (webhook lag, silent lapses, card testing)
   ```
3. **URL:** `https://[YOUR_LIVE_URL]?ref=hn`
4. Submit.
5. **Immediately post your first comment** (the body text from SHOW_HN_FINAL.md) as a reply to your own post.

---

## T+5 MIN (9:05 AM) — Cross-Post

In order of priority:
1. **Indie Hackers** — https://www.indiehackers.com/post (use IH template from BETA_OUTREACH.md)
2. **r/SaaS** — https://reddit.com/r/SaaS (use Reddit template from BETA_OUTREACH.md)
3. **Dev.to** — https://dev.to/new (use Dev.to template)

---

## If Cloudflared Isn't Installed

```bash
# Check if installed
which cloudflared

# If not: install it (LuLu will prompt — allow it)
brew install cloudflare/cloudflare/cloudflared

# First run will trigger LuLu alert — click Allow
cloudflared tunnel --url http://localhost:8080
```

If LuLu blocks and you can't add the rule:
- Switch to Option B (persistent CF deploy) — needs CF_API_TOKEN and CF_ACCOUNT_ID
- Get tokens at: https://dash.cloudflare.com → My Profile → API Tokens

---

## Emergency Fallback (no live URL by 9AM)

Post the Show HN WITHOUT a URL (HN allows text-only posts).
Use "Ask HN" format instead:

```
Ask HN: Would you use a self-hosted Stripe anomaly detector?
```

Then paste the key points as the body. Less ideal but still gets discussion.
Ask Lucky to help reformulate the post text.

---

## After Posting

Keep an eye on HN — respond to every comment in the first 2 hours. Fast replies signal an engaged founder and help with HN ranking.

Tell Lucky when it's live so the activity log can be updated.

---
*Generated by Lucky — March 10, 2026 1:18 AM*
*Artifact: ~/Projects/BillingWatch/docs/SHOW_HN_PREP_CHECKLIST.md*
